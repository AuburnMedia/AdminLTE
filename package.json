{
  "name": "ecotrack-static-html",
  "version": "1.0.0",
  "description": "EcoTrack Carbon Dashboard - Static HTML Project",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "dev": "python3 -m http.server 8000",
    "serve": "python3 -m http.server 8000",
    "setup": "echo 'No setup needed - this is a static HTML project'",
    "install": "echo 'No dependencies to install - using CDN resources'",
    "build": "echo 'No build process needed - static HTML project'"
  },
  "keywords": [
    "html",
    "css", 
    "javascript",
    "static",
    "carbon-tracking",
    "adminlte",
    "charts"
  ],
  "author": "AuburnMedia",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "devDependencies": {},
  "dependencies": {},
  "comments": {
    "note": "This package.json exists only to prevent npm install errors.",
    "dependencies": "All JS/CSS dependencies are loaded via CDN in HTML files",
    "development": "Use 'python3 -m http.server 8000' to serve files",
    "deployment": "Deploy static files to any web server"
  }
}